import {Validators} from '@angular/forms';

export class Product {
    item_name: string;
    item_desc: string;
    item_keys: string;
    item_quantity: number;
    item_price: number;
    imageFile_1: [''];
    imageFile_2: [''];
    imageFile_3: [''];
    imageFile_4: [''];
}

